import { ContainerSect } from "./styled";

const ContainerSections = ({children}) => {
    return (
        <ContainerSect>
            {children}
        </ContainerSect>
    )
}

export default ContainerSections;