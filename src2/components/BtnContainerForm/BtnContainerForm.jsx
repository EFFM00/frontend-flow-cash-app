import { BtnContainer } from "./styled";

const BtnContainerForm = ({children}) => {
    return (
        <BtnContainer>
            {children}
        </BtnContainer>
    )
};

export default BtnContainerForm;